# START PACK: .gitignore (civ7-tracker)
# OS cruft
.DS_Store
Thumbs.db
desktop.ini

# Editor/IDE
.vscode/
.idea/

# Node & tooling
node_modules/
npm-debug.log*
yarn-error.log*
pnpm-debug.log*

# Local servers / temp
*.local
*.tmp
tmp/
temp/

# Git hooks output (keep logs local-only)
.githooks/*.log
.githooks/*.out
precommit_log.txt

# Build artifacts (future-proof; static site stays clean)
dist/
build/
coverage/

# markdownlint output (add these lines to your existing .gitignore)
**/markdownlint.out

# Supabase config: Now committed to repo for GitHub Pages deployment
# The anon key is safe to be public - it's designed for client-side use with RLS policies
# js/supabase-config.js (committed for public deployment)

# END PACK: .gitignore (civ7-tracker)
